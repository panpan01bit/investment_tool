# Auto-generated requirements for macro-bot (including daily-news-fetcher)
# Source: 14 Python files under macro-bot/; local/stdlib modules removed.
# Pinned to secure minimum versions; run `pip install -r requirements.txt` on server.
# Note: akshare is required by daily-news-fetcher/akshare_fetcher.py but shadowed
# by a local directory named 'akshare'. Consider renaming the data directory to
# avoid import conflicts in production.
akshare>=1.15.0
flask>=3.0.3
flask-cors>=4.0.1
pandas>=2.2.2
python-dotenv>=1.0.1
requests>=2.32.3
werkzeug>=3.0.3
